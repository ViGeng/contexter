name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild \
            -project contexter.xcodeproj \
            -scheme contexter \
            -configuration Release \
            -destination 'platform=macOS' \
            BUILD_DIR=$PWD/build \
            build
